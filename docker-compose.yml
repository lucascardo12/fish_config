version: "3.9"
services:
  fish:
    build: .
    ports:
      - "8080:8080"
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ~/apps/mongo:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=seunome
      - MONGO_INITDB_ROOT_PASSWORD=suasenha
